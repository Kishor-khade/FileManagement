# File Management Application

Steps to run the application
- $`pip install -r requirements.txt`
- $`python manage.py makemigrations` Not needed just to make sure all are ok
- $`python manage.py migrate` Not needed just to make sure all are ok
- $`python manage.py runserver`

- Here You can go.....
